using PromptUGUI.IR;
using UnityEngine;
using UnityEngine.EventSystems;

namespace PromptUGUI.Application.Navigation
{
    /// <summary>光标 overlay 的视图组件（Task 7 填充行为；本版本为空占位）。</summary>
    internal sealed class FocusCursorView : UIBehaviour
    {
        internal void Init(Screen screen, RectTransform overlay, ElementNode cursorNode)
        {
            // Task 7 fills in cursor tracking and visibility logic.
            _ = screen;
            _ = overlay;
            _ = cursorNode;
        }
    }
}
